# CONTEXT — 도메인 용어집

이 저장소에서 도메인 개념을 지칭하는 말을 **한 가지 의미로** 고정한다.
이슈 제목, 테스트 이름, 화면 문구, 커밋 메시지, 규정 저작에서 여기 어휘를 쓴다.

**이것은 글로서리이지 스펙이 아니다.** 구현 세부는 `docs/adr/`와 ARCHITECTURE 문서가 갖는다.
무대(회사·조직·도구)는 [`docs/company.md`](docs/company.md)에 있다.

## 항목의 형식

| 칸 | 뜻 |
| --- | --- |
| **정의** | 이 저장소에서 이 말이 가리키는 단 하나의 의미 |
| **채택 출처** | 그 의미를 어디서 가져왔는가. #14가 **한국 결재 문법에 표준이 없음**을 1차 자료로 확정했으므로, 결재 용어는 이식이 아니라 **선택**이다. 무엇을 골랐는지가 기록되지 않으면 그 선택은 근거를 잃는다 |
| **상태 전이** | 이 말이 가리키는 것이 어디서 와서 어디로 가는가. 스펙이 아니라 **의미 그 자체**다 — `반려`는 "어디로 돌아가는가"가 곧 정의다 |
| **반례** | 이 말로 부르지 **않는** 것, 또는 이 뜻으로 쓰지 않는 다른 말 |

### 출처 등급

`[1차·법령]` `[1차·기관규정]` `[1차·벤더]` `[1차·스니펫]` `[2차]`는 #14 조사의 등급 표기를 그대로 쓴다.
**`[1차·스니펫]`은 본문 직접 확인에 실패하고 검색 스니펫만 확보한 것이다** — `[1차·벤더]`와 섞지 않는다.

확보한 법령 원문은 2021년판이고, 기관 규정·벤더 조사에는 미확보 구간이 있다.
아래 인용은 **#14가 확보한 원문과의 일치**를 뜻하며, 현행 법령이나 현재 제품을 독립 검증한 것이 아니다.

---

# §1 결재 문법

가상 회사는 `한성워크`라는 그룹웨어를 쓴다(`docs/company/tools.md`).
**어느 벤더의 의미론도 통째로 채택하지 않는다** — 용어마다 고르고 출처를 남긴다. 근거는 [ADR-0001](docs/adr/0001-approval-semantics-per-term.md).

### 기안

- **정의**: 출장 당사자가 출장 계획 또는 정산 내역을 문서로 만들어 결재선에 올리는 행위. 그 문서를 만든 사람이 **기안자**이고, 그 결과물을 **기안 문서**라 부른다.
- **채택 출처**: 안산대 사무관리규정 제27조① `[1차·기관규정]` — "기안한 문서는 결재권자의 결재를 받아야 효력이 발생한다". **대통령령 「행정업무의 운영 및 혁신에 관한 규정」 제10조①은 다른 문장이다** — "문서는 해당 행정기관의 장의 결재를 받아야 한다" `[1차·법령]`. 법령은 결재의 **필요**를 규정하고, 효력 발생을 명문화한 것은 기관 규정 쪽이다.
- **상태 전이**: (없음) → `상신`
- **반례**: `신청`이라 부르지 않는다. 신청은 승인 여부가 열려 있는 요청이지만, 기안은 **문서**이고 결재선을 동반한다. 이 구별이 사라지면 전결표가 붙을 자리가 없어진다.

### 결재선

- **정의**: 한 기안 문서가 통과해야 하는 결재자의 순서. `(문서유형, 출장 유형, 금액, 상향 사유)`와 기안자의 **소속·직책**으로 결정되며, 정산이면 그 출장 사전승인의 전결권자가 하한으로 더해진다. 사람이 그때그때 고르지 않는다.
- **채택 출처**: 전결표로 고정하는 형태는 #14 §5~§6이 확인한 실무 운영 형태 `[1차·기관규정]`. 입력 목록은 #16이 전결표를 저작하며 확정했다([`docs/company/approval-matrix.md`](docs/company/approval-matrix.md) `APV-2-2`·`APV-8-1`) — **금액만으로는 "금액은 크지 않은데 한 달을 비우는 출장"이 갈리지 않았다.** 그 출장은 기간이 아니라 **출장 유형의 행**이 가른다 — 해외 사업장 방문은 금액과 무관하게 본부장부터 시작한다(`APV-5-2`, 2026-09-21 #16 교차 대조 결정 D-3). 정산의 하한(`APV-7.2-1`)은 [ADR-0006](docs/adr/0006-limit-excess-moves-approval-once.md).
- **상태 전이**: 기안 시점에 **전결표에서 계산되어 확정**된다. 진행 중 변경되지 않으며, 결재선을 바꾸는 수정은 재기안으로만 한다(`APV-11`).
- **반례**: 기안자가 결재자를 직접 지정하는 방식은 쓰지 않는다. 그러면 판정 근거가 규정 원문이 아니라 사람의 선택이 되어, 이 MVP의 차별점이 사라진다. **기안자 본인도 자기 문서의 결재자가 되지 않는다**(`APV-8-2`).

### 전결

- **정의**: 대표이사가 `(문서유형, 출장 유형, 금액)`에 따라 하위 **직책자**에게 **사전에 규범으로** 결재권을 위임하여, 그 직책자가 최종 결정하는 것. 위임의 내용은 **전결표**라는 원문에 적혀 있다.
- **채택 출처**: 대통령령 제10조② `[1차·법령]` — "그 위임전결 사항은 해당 기관의 장이 훈령이나 지방자치단체의 규칙으로 정한다". 안산대 제27조②④ `[1차·기관규정]`. #14의 **정의 A**를 채택한다. 다우오피스 `[1차·벤더]`·네이버웍스 `[1차·스니펫]`의 **정의 B**("결재선을 단축해 그 자리에서 종결하는 기능")는 **채택하지 않는다** — 정의 B는 사람이 그때그때 단축하는 기능이라 판정 근거로 인용할 원문이 없다.
- **상태 전이**: `진행중` → (전결권자 승인) → **`완료`**. 전결권자 뒤의 결재자는 발생하지 않는다 — 결재선이 애초에 거기까지만 계산되기 때문이다.
- **반례**: "전결로 건너뛴다"고 말하지 않는다. 건너뛸 결재자가 존재한 적이 없다.

### 전결표

- **정의**: `(문서유형, 출장 유형, 금액) → 결재권자 직책`을 정하는 사내 규정 원문. 금액은 **총액**과 **한도 적용 금액** 둘로 보고, **상향 사유**는 한도 적용 금액 쪽 단계만 한 칸 올리며, 두 단계 중 높은 쪽을 쓴다([ADR-0006](docs/adr/0006-limit-excess-moves-approval-once.md)). 에이전트가 판정 근거로 **조항 단위로 인용**한다.
- **채택 출처**: #14 §5~§6 `[1차·기관규정]`. 확인된 사례 — 이화여대의 금액 계단(100만원 / 2,000만원)은 `지출결의` 기준이고, KNOC은 출장을 관내/관외로 갈라 팀장/부서장에게 준다(§6). **KHIDI의 기간 계단(1일/1주일/1개월)은 출장이 아니라 `국내교육훈련` 항목의 것이었다** — #16이 원문(일부개정 2019. 2. 20 별표)을 받아 확인했고, 같은 별표의 출장 행은 `출장자 직위 × 지역`으로 갈린다 `[1차·기관규정]`. 확인한 네 기관 어디에도 출장을 기간으로 가르는 행이 없어, **기간 축은 없앴다**(`APV-6` 삭제, 2026-09-21 #16 교차 대조 결정 D-3). 그 역할은 출장 유형의 행이 맡는다.
- **상태 전이**: (정적) — 실행 중 바뀌지 않는다. 저작은 #16이 마쳤다([`docs/company/approval-matrix.md`](docs/company/approval-matrix.md)).
- **반례**: **직급**을 축으로 쓰지 않는다. 전결표의 축은 **직책**(팀장·본부장·대표이사)이다. 여비 한도만 직급 축을 쓴다. 이 둘이 다른 축이라는 것이 이 도메인의 실제 구조다 — `docs/company/org.md` 참조. 단 **한도 초과가 결재권자를 한 단계 올리는 연쇄**(#5 결정 5)는 두 축을 맞물리게 하므로, 무관한 분류표 둘로 구현하지 않는다.

### 재무합의

- **정의**: 재무팀이 증빙과 정산 내역을 검토하여 **동의해야만** 다음 단계로 넘어가는 차단형 절차. 재무합의자는 **항상 반려할 수 있다**. 순서는 **순차**다.
- **채택 출처**: 이름과 순차성은 하이웍스 `[1차·벤더]` — "합의와 다르게 재무합의는 순서대로 결재". 반려 권한의 무조건성도 하이웍스 — 일반 `합의`는 "합의자는 반려를 허용하지 않을 수 있습니다"라는 **관리자 설정값**인데 재무합의는 그렇지 않다. 차단성(반대 시 문서가 반려됨)은 KT 비즈메카의 **정의 B** `[1차·벤더]`. **하이웍스의 결재선상 위치는 채택하지 않는다** — 하이웍스 합의자는 "결재가 모두 끝난 후 의사를 표현"하지만, 그러면 #5가 정한 '척추 = 승인자 큐'와 충돌한다. 결재선 **중간**에 놓는다(다우오피스·비즈메카 방식).
- **결재선에서의 위치**: **기안자와 첫 결재자 사이**다(`APV-9-1`). 정산 문서에만 붙고 사전승인에는 붙지 않는다. 재무합의자는 재무팀 담당자와 재무팀장 둘이며 청구 총액에 따라 순차로 선다(`APV-9-2`). 위치를 여기로 고정한 이유는 ⑴ 전결권자의 승인이 마지막이어야 하고 ⑵ 결재선의 길이가 전결 단계에 따라 변해 "중간"이 계산 가능한 자리가 아니기 때문이다.
- **상태 전이**: `상신` → `재무합의대기` → (동의) `진행중` / (반려) → **`반려`** / (보류) → `보류`. 재무합의자도 결재권자와 같이 **동의 · 반려 · 보류** 셋을 쓴다([ADR-0003](docs/adr/0003-approver-actions-three.md)의 "역할과 액션은 다른 축"을 따른 것이고, `APV-9-5`가 조항이다).
- **설정값 노출**: #5 결정 8이 "표준이 없으므로 **이 선택 자체를 설정값으로 노출한다**"고 확정했다(#14 제안 ②). 위 세 성질(순차 · 차단 · 항상 반려 허용)은 고정 상수가 아니라 **드러난 설정값**이고, **그 값이 사는 자리는 전결표의 조항 `APV-9-3`이다**([ADR-0005](docs/adr/0005-policy-source-of-truth.md)). 화면은 판 1의 세 값을 `(APV, 판, APV-9-3)` 인용과 함께 읽기 전용으로 보여 주고, 값은 규정 개정권자가 새 판을 게시해야만 바뀌며, 엔진은 판 1의 조합만 실행한다([ADR-0014](docs/adr/0014-apv-9-3-exposed-declaratively.md) 결정 1–3). 개정권자는 대표이사다([ADR-0022](docs/adr/0022-policy-amendment-by-ceo-no-in-app-publishing.md)) — 규정을 개정할 권한이 곧 이 설정을 바꿀 권한이 된다. admin은 감사자이므로 이 설정의 변경권을 갖지 않는다.
- **반례**: `합의`라고 줄여 쓰지 않는다. #14가 확인한 대로 `합의`는 세 가지 양립 불가능한 뜻(비차단 / 차단 / 에스컬레이션)으로 갈리므로, 항상 `재무합의`로 적는다. `협조`라고 부르지도 않는다 — #5가 확정한 대로 협조는 **법령상 반려 권한이 없어** 이 역할을 못 맡는다(대통령령 제9조②·시행규칙 제6조③ `[1차·법령]`).

### 승인

- **정의**: 결재권자가 기안 문서에 동의하여 다음 단계로 넘기는 행위. 결재선의 마지막 승인이 문서의 **효력을 발생**시킨다.
- **채택 출처**: 효력 발생은 안산대 제27조① `[1차·기관규정]`. 마지막 결재자의 승인이 절차를 끝낸다는 것은 다우오피스 `[1차·벤더]` — "최종 결재자가 승인하면 결재 프로세스는 완료". 결재 자체의 필요는 대통령령 제10조① `[1차·법령]`.
- **상태 전이**: `결재대기` → (마지막 결재자가 아님) `진행중` / (마지막 결재자) → **`완료`**
- **반례**: `결재`와 `승인`을 섞어 쓰지 않는다. **`결재`는 제도**(결재선·전결표가 구성하는 체계)를, **`승인`은 한 사람의 한 번의 행위**를 가리킨다.

### 반려

- **정의**: 결재권자 또는 재무합의자가 기안 문서를 승인하지 않고 되돌리는 행위. 기안자가 고쳐서 다시 기안해야 한다. **건 전체가 단위다.**
- **채택 출처**: 다우오피스 `[1차·벤더]`, 카카오워크 `[1차·스니펫]`. **법령 용어가 아니다** — 대통령령 전문 출현 0회(#14). 그럼에도 #5가 결재 역할의 최소 집합에 넣은 것은, 인사·총무 실무에서 실제로 도는 것이 `전결`과 `반려` 둘뿐이기 때문이다.
- **상태 전이**: `진행중` → **`반려`** → 기안자에게 전달. 기안자는 고쳐 **재상신**하고 결재선의 처음부터 다시 받는다 — 반려 전의 승인은 잇지 않는다. 반려된 사전승인은 **`철회`**할 수도 있고, 철회는 출장 취소로 본다. 정산은 철회하지 못한다([ADR-0024](docs/adr/0024-rejection-resubmission-and-card-bound-approvals.md), [ADR-0025](docs/adr/0025-withdrawal-only-for-preapproval.md)).
- **반례**: `거절`·`거부`라고 쓰지 않는다. **부분 반려**(KT 비즈메카의 수신부서별 반려 `[1차·벤더]`)는 채택하지 않았다 — 상태가 항목 단위로 쪼개지기 때문이다. 단 이것이 #19가 요구한 **항목별 부분 승인**까지 배제한 것은 **아니다**. 둘은 다른 물건이다. #17은 **항목별 부분 승인도 채택하지 않았다** — 재량 "인정하지 않음"은 문서 하나의 금액 결과다([ADR-0024](docs/adr/0024-rejection-resubmission-and-card-bound-approvals.md) 결정 10–11).

### 보류

- **정의**: 승인자(결재권자·재무합의자)가 승인(재무합의자는 `동의`)도 반려도 하지 않고, **기안자 또는 에이전트에게 무언가를 물어보는** 상태. 문서는 진행을 멈추고 그 승인자의 큐에 남는다.
- **채택 출처**: 승인/반려 밖에 제3의 액션이 실재한다는 것은 하이웍스 `협의 요청` `[1차·벤더]`("결재자가 보류 상태로 협의를 요청")과 WEHAGO `검토상태` `[1차·스니펫]`("검토할 시간이 필요한 경우")로 확인된다. **전이 규칙은 둘 중 어느 쪽도 채택하지 않았다 — 이 회사의 선택이다.** 하이웍스는 "협의 요청하면 결재 진행은 **중단되지 않고**" 상위 결재자가 선결하면 후열로 바뀌지만, 우리 보류는 **진행을 멈춘다**. 하이웍스 매뉴얼은 협의의 상대를 확정하지 않으며, #14 §12는 오히려 "기안자에게 문서 내용 수정을 요청"이라고 요약한다 — **상대를 기안자·에이전트로 한정하는 것도 우리 선택이지, 하이웍스와의 차이가 아니다.**
- **상태 전이**: `결재대기`(재무합의자면 `재무합의대기`) → **`보류`** → (입력을 바꾸지 않은 답변 도착) → 같은 대기 상태(같은 승인자). 입력을 바꾼 답변은 결재선이 바뀌면 `반려`로 가고([ADR-0024](docs/adr/0024-rejection-resubmission-and-card-bound-approvals.md) 결정 5의 `[설계 가정]`), 이미 승인한 단계의 카드가 바뀌었으면 바뀐 가장 앞 단계로 되돌리며, 둘 다 아니면 같은 대기로 돌아간다. 답변 뒤 다시 그 승인자의 차례가 된 날이 새 도달일이다(`PRC-14-2`, `docs/company/travel-procedure.md:283`). 승인자는 답을 기다리지 않고 보류 중에도 승인(동의)·반려할 수 있다. **시간이 흘러도 상태는 바뀌지 않는다** — 기안자에게 물은 보류에는 답변 기한을 두고(값은 #20), 그 기한이 지나면 그다음 영업일이 승인자의 새 도달일이다([ADR-0026](docs/adr/0026-hold-answer-deadline-time-changes-no-state.md)).
- **반례**: 승인/반려 이분법으로 뭉개지 않는다. #14 §7이 1차 자료로 확인한 대로 **보류는 이미 실제 제품의 1급 상태**이고, 이 MVP의 판단 봉우리 둘(사전승인·정산)에서 "증빙이 모자란다 / 규정 해석이 갈린다"는 반드시 발생한다. 반려는 과하고 승인은 틀린 구간이 실재한다.

### 이 저장소가 쓰지 않는 결재 용어

#14가 확인한 대로 아래 낱말들은 출처마다 양립 불가능한 뜻을 갖는다. **쓰지 않는다.**

| 안 쓰는 말 | 이유 |
| --- | --- |
| `후결` | **서로 양립하지 않는 6가지 용례**가 확인됐다(#14 §4.1). 출처 구성은 벤더 5곳 — 다우오피스·KT 비즈메카·하이웍스·이카운트 `[1차·벤더]`, WEHAGO `[1차·스니펫]` — 에 2차 해설 1건(ihee.com `[2차]`)이다. 법령·기관 규정 어디에도 없다 |
| `후열` | 다우오피스에서는 대결에 종속되지만(`대결로 완료` → 원 결재자가 추후 열람), 하이웍스에서는 **선결·협의 요청 해제로도 후열이 발생한다** `[1차·벤더]`. 뜻이 갈리고, 우리는 대결·선결·협의 요청을 모두 쓰지 않아 가리킬 대상이 없다 |
| `선결` | 결재 순서를 앞당겨 건너뛰는 기능. 결재선을 전결표에서 계산하는 이 저장소에는 건너뛸 대상이 없다 |
| `대결` | 부재 대리(대통령령 제10조③ `[1차·법령]`). 실재하는 법령 개념이지만 MVP 범위 밖이다. 넣으려면 ADR로 다시 연다 |
| `합의` (단독) | 세 가지 양립 불가능한 뜻으로 갈린다. 항상 `재무합의`로 적는다 |
| `협조` | 법령상 반려 권한이 없어 재무의 증빙 검토를 못 맡는다(#5) |
| `공람`·`참조` | 결재 권한 없이 열람만 하는 유형. 승인자 큐에 자리가 없다 |
| `확인`·`감사` (결재유형) | **비차단 유형이라서가 아니다** — 다우오피스의 `확인`은 "결재 기능과 동일하게 결재 또는 반려가 가능하지만, 해당 액션은 결재선에 노출되지 않음"이고, `감사`는 반려가 기본 금지지만 "관리자 설정에 따라 반려 액션을 허용할 수 있음"이다 `[1차·벤더]`. 즉 둘 다 실제로는 차단할 수 있다. 쓰지 않는 이유는 **결재 행위를 결재선에서 감추기 때문**이다. 이 MVP는 행위자·책임자를 드러내는 것이 목적이라 정면으로 어긋난다 |

---

# §2 출장 도메인

### 출장

- **정의**: 직원이 통상 근무지를 벗어나 업무를 수행하고 돌아오는 **하나의 사건**. 출발 전 `사전승인`부터 복귀 후 `정산`과 그 뒤 재무팀의 지급 처리까지가 한 건의 생애다.
- **채택 출처**: #5 — 대상을 '업무'가 아니라 '**사건**'으로 잡는다. 팀 라벨은 회사마다 흔들리지만 사건은 흔들리지 않는다(#2).
- **상태 전이**: `사전승인` → `출장 수행` → `정산` → `완료`(지급 처리 뒤)
- **반례**: `출장 신청`과 `출장 정산`을 별개의 두 사건으로 부르지 않는다. **한 사건의 두 봉우리**다 — 이 구별이 무너지면 #5 결정 10이 "경계는 전체 생애"라고 정한 것이 무의미해진다.

### 사전승인

- **정의**: 출장을 떠나기 전에, 출장 계획(목적·기간·행선지·예상 비용)을 **여비규정·전결표·출장관리규정**에 대조받아 결재받는 것. **판단 봉우리 ①**. 기안과 결재 완료에 각각 기한이 걸린다(`PRC-5`, `PRC-6`).
- **채택 출처**: #5 결정 10 — 판단 봉우리는 사전승인과 정산 두 곳이고, 예약·출장 중 구간은 상태 전이로만 둔다.
- **상태 전이**: `기안` → `결재선 진행` → `완료`(예약 착수 가능) / `반려` / `보류`. `반려`에서는 재상신하거나 **`철회`**한다([ADR-0025](docs/adr/0025-withdrawal-only-for-preapproval.md)).
- **반례**: `출장 신청`이라 부르지 않는다. 승인 대상은 **계획**이지 신청서가 아니다.

### 정산

- **정의**: 출장에서 돌아와 실제 지출을 증빙과 함께 제출하고, 여비규정·전결표에 대조받아 결재받는 것. **판단 봉우리 ②**. 재무합의가 여기에 있다.
- **채택 출처**: #5 결정 10. 가장 중요한 판단은 봉우리 하나가 아니라 **둘 사이의 차이**다 — "승인은 이코노미였는데 비즈니스로 갔다", "숙박 3박 승인인데 4박이 결제됐다".
- **상태 전이**: `기안` → `재무합의대기` → `결재선 진행` → `완료` / `반려` / `보류`. `완료` 뒤에 재무팀의 **지급 처리**가 온다 — 지급 기한(`PRC-16-1`)을 닫는 상태 전이이고 결재가 아니다. 출장의 `완료`는 지급 처리 뒤다. 지급 처리는 순액을 기록한다. 순액이 음수면 문서는 **`반환 대기`**에 머물고, 재무팀의 **반환 확인**으로 `지급 처리됨`이 된다([ADR-0028](docs/adr/0028-negative-net-goes-to-repayment-pending.md)).
- **반례**: `경비 청구`라 부르지 않는다. 청구는 금액만 다루지만 정산은 **사전승인과의 대조**를 포함한다. **정산 `완료`는 지급이 아니다** — `완료`는 결재선의 끝이고, 지급은 그로부터 기한이 걸리는 별개의 사건이다(`PRC-16-1`).

### 사전승인과 정산의 차이

- **정의**: 같은 출장의 **사전승인 문서**와 **정산의 확정값**이 항목 단위로 달라진 것. 차이마다 사유와 적용 조항이 붙어 결재권자의 판정 입력이 된다(`APV-10-2`). 무엇을 대조하는지의 최소 목록은 [PRD](docs/PRD.md)의 핵심 기능 4가 정한다.
- **채택 출처**: #5 결정 10 — 가장 중요한 판단은 봉우리 하나가 아니라 **둘 사이의 차이**다(위 `정산`). 이름은 전결표 원문 그대로다 — `APV-10-2` 제2호 "사전승인과 정산의 차이와 그 사유". 기안자가 정산 문서에 적는 쪽은 `PRC-11-1` 제3호 "사전승인과 달라진 내용과 그 사유"다.
- **상태 전이**: `정산` 확정값 → (같은 출장의 사전승인 문서와 대조) → 결재권자의 판정 입력. 사전승인이 완료된 뒤 정산 전까지는 결재 단계가 없으므로(`PRC-9-2`) 그사이에 생긴 변경은 — 출발 전(예약 포함)이든 출장 중이든 — 정산에서 **사후에만** 잡힌다. 변경 사실과 사유가 정산 문서에 적히고(`PRC-9-1`) 이 차이로 결재권자에게 닿으며, 늘어난 청구 총액은 총액 단계(`APV-5`)에도 반영된다([ADR-0004](docs/adr/0004-three-policy-documents.md)). 예약 때 생긴 차이에는 연결 때 남긴 벗어난 항목과 사유(`PRC-8-1`)가 붙어, 그 차이가 어느 시점에 누구 손으로 생겼는지 가려진다.
- **반례**: **`차액`이 아니다** — 차액은 대사에서 증빙 금액과 카드 줄 금액이 어긋난 값이고, 이것은 두 문서 사이의 변경이다. **총액이 같다고 차이가 없는 것이 아니다** — 좌석 등급만 바뀐 출장이 있다. **자료가 없는 칸을 `변경 없음`으로 채우지 않는다** — 모름과 같음은 다른 값이다.

### 인계

- **정의**: 한 출장이 인사·총무·재무 가운데 한 팀의 손을 떠나 다음 팀의 일이 되는 지점. 인사·총무 사이의 인계(예약 착수·예약 확정·근태 반영)는 **결재가 아니라 상태 전이**다. 재무로의 인계는 정산 `상신`이고, 그 뒤의 판단은 결재선 위의 재무합의가 맡는다(`APV-9-1`).
- **채택 출처**: #2 — 두 팀 사이의 인계는 어느 제품도 제품 기능으로 모델링하지 않았고, 그래서 대상은 업무가 아니라 사건이어야 했다. #5 결정 2 — 시스템 분산이 원인이고 **팀 인계 실패가 증상**이다. 인계 지점은 as-is 시스템의 틈 셋과 하나씩 대응한다([`docs/company/tools.md`](docs/company/tools.md)). 예약·근태 반영이 결재가 아니라는 것은 `PRC-8-3`이 조항으로 적었다.
- **상태 전이**: 사전승인 `완료` →(총무) 예약 착수(`PRC-4-2`) → 예약 확정 → 사전승인 문서 연결, 범위를 벗어난 항목과 사유 기록(`PRC-8-1`) →(인사) 근태 반영(`PRC-8-2`) → 출장 수행 → 복귀 → 정산 기안(기한은 복귀일부터 센다, `PRC-10-1`) → `상신` →(재무) `재무합의대기`. **총무팀 예약이 없는 출장**(당일 국내 출장 등)은 예약을 건너뛰고, 인사팀이 사전승인된 일정으로 근태를 반영한다(`PRC-8-2`).
- **반례**: **인계가 결재선을 새로 만들지 않는다** — 예약·근태 반영에는 결재선이 생기지 않고(`PRC-8-3`), 재무로의 인계는 정산 문서가 이미 가진 결재선을 따라간다. 예약 확정 때의 범위 대조도 **기록이지 판단이 아니다** — 판단은 정산의 `사전승인과 정산의 차이`에서 한다(`PRC-9-2`). **예약은 출장의 상태가 아니다** — 있을 때만 일어나는 인계다. 퇴사·발령 때의 **업무 인수인계**와 다른 말이다 — 넘어가는 것은 사람의 업무가 아니라 한 출장 건이고, 그 사건들은 #5에서 탈락했다.

### 여비

- **정의**: 출장에 대해 회사가 지급하는 금액의 총칭. **여비규정**이 직급별·지역별 한도를 정한다.
- **채택 출처**: #16이 원문을 저작해 확정했다([`docs/company/travel-policy.md`](docs/company/travel-policy.md)). 한도의 형태는 `직급 × 지역 등급` 표이고, 한도를 넘은 부분은 본인 부담이며 부득이한 사유가 있을 때만 지급을 청구한다(`TRV-11`, `TRV-12`).
- **상태 전이**: (정적)
- **반례**: `경비`와 섞지 않는다. 경비는 회계 계정과목이고, 여비는 **규정이 한도를 정하는 지급액**이다.

### 일비 / 숙박비 / 교통비 / 등록비

- **정의**: 여비의 항목. **일비**는 증빙 없이 업무일 수에 따라 정액 지급되는 몫, **숙박비**·**교통비**는 증빙에 대조되는 실비, **등록비**는 전시회 참가에 드는 실비다(`TRV-4`~`TRV-9`).
- **채택 출처**: 저작(#16). **이 네 항목의 구성은 이 저장소의 선택이고, 기관 규정의 공통 골격이라는 주장은 하지 않는다** — #16도 기관 여비규정 원문을 확보하지 못했다. 식비를 따로 두지 않고 일비에 넣은 것은 아래 반례를 따른 결과이며, 그 대가로 "법인카드로 결제한 본인 식대를 일비에서 뺀다"는 조항이 필요해졌다(`TRV-5-3`).
- **상태 전이**: (정적)
- **반례**: 일비를 "식비"라 부르지 않는다. 일비는 **증빙을 요구하지 않는다**는 점이 정의의 핵심이고, 그래서 증빙 파이프라인이 일비를 건드리지 않는다.

### 환율 기준일

- **정의**: 외화로 지출한 여비를 원화로 환산할 때 적용하는 매매기준율의 **날짜**. 이 회사는 **지출일**로 정했다(`TRV-14-1`).
- **채택 출처**: #15 — **법령에 명문 규정이 없다**(법인세법 §43이 기업회계기준으로 넘긴다) `[1차·법령]`. 세법 실무가 지지하는 날짜는 **발생일**이고(법인세법 기본통칙 42-76…2 1호 `[1차·기관]`), **카드 청구일도 사내 정산일도 어느 경로로도 지지되지 않는다.** 그 통칙의 대상은 외화자산·부채이지 비용이므로 준용을 명령하는 문언은 없다 — 지출일 채택은 **회사 정책**이고, 정책이므로 시스템 설정값이 아니라 조항이다.
- **상태 전이**: (정적) 지출 한 건마다 그 지출일로 정해진다. 지출일에 매매기준율이 고시되지 않았으면 직전 고시일(`TRV-14-2`).
- **반례**: `정산일`·`카드 청구일`이라 부르지 않는다. **회계 장부에 적을 원화 금액과도 같은 말이 아니다** — 이 환산은 한도 대조와 지급액 산정에만 쓴다(`TRV-14-3`).

### 지출일

- **정의**: 증빙에 적힌 거래 일자. 카드로 결제한 지출은 카드 매출전표의 거래 일자(`TRV-3-5`).
- **채택 출처**: 저작(#16).
- **상태 전이**: (정적)
- **반례**: 결제 승인일·카드사 매입일·청구일과 구별한다.

### 지역 등급

- **정의**: 여비 한도를 가르는 국가 묶음. `가`(독일·일본) · `나`(멕시코) · `다`(베트남) · `국내` 넷이며 **국가 단위**로 고정한다(`TRV-3-4`).
- **채택 출처**: 저작(#16). 무대의 출장지 4개국(`docs/company.md`)에 맞춘 것이다.
- **상태 전이**: (정적)
- **반례**: 도시 단위로 쪼개지 않는다. 등급표에 없는 곳에서의 경유지 숙박은 `가` 등급을 적용한다.

### 출장 일수 / 업무일 / 비업무일

- **정의**: **출장 일수**는 출발일부터 복귀일까지의 달력 일수(한국 시간 기준 날짜). **업무일**은 ⑴ 그날의 업무 수행 내용이 적힌 날 ⑵ 출발일과 복귀일 ⑶ 업무일 사이에 낀 회사의 휴일이고, **비업무일**은 나머지다(`TRV-3-2`, `TRV-3-3`).
- **채택 출처**: #15 — 국세청 기본통칙 19-19…25의 안분 단위가 **기간**이므로 날짜 구분이 판정의 입력이 된다 `[1차·기관]`. 날짜 구분의 보존은 #18, 구분의 규칙과 효과는 #16.
- **상태 전이**: 사전승인에 계획으로 적히고, 정산에서 실제로 확정된다.
- **반례**: 근태의 근무일과 같지 않다. 비업무일이라고 해서 휴가가 되는 것도 아니다. 출장 일수는 업무일 수와 다르다 — 일비는 업무일에만 걸린다. 출장 일수는 전결의 입력이 아니다(전결표의 기간 축은 없앴다).

### 사적 일정

- **정의**: 출장 기간에 겸한 업무 외 일정. 비업무일을 만들고, 그 날의 일비·숙박비를 지급하지 않게 한다(`TRV-13`).
- **채택 출처**: 기본통칙 19-19…25가 "업무수행상 필요하다고 인정할 수 없는 여행을 겸한 때"를 다룬다 `[1차·기관]`.
- **상태 전이**: 사전승인 문서에 적는다. 적지 않은 사적 일정이 정산에서 드러나도 같은 조를 적용한다(`TRV-13-5`).
- **반례**: 휴가와 다르다. 사적 일정은 출장 한 건 안에서 다뤄진다.

### 한도 적용 금액 / 청구 총액

- **정의**: **한도 적용 금액**은 여비 청구액 중 한도를 넘지 않는 부분의 합계이고(`TRV-3-7`), **청구 총액**은 거기에 한도 초과 청구분까지 더한 금액이다. 사전승인에서는 같은 금액을 예상액으로 계산하며 **예상 총액**이라 한다. 전결은 **둘 다 본다** — 총액 단계와, 상향을 거친 한도 적용 금액 단계 중 높은 쪽(`APV-5-1`, `APV-7-4`). 재무합의의 단계는 청구 총액을 쓴다.
- **채택 출처**: 저작(#16)과 2026-09-21 #16 교차 대조 결정(D-1, [ADR-0006](docs/adr/0006-limit-excess-moves-approval-once.md)). 둘을 가른 이유는 한도 초과 청구가 결재선을 **한 번만** 움직이게 하려는 것이다 — 둘을 더하면 초과가 금액 계단과 상향을 이중으로 부르고, 한도 적용 금액만 보면 초과가 아무리 커도 한 단계에 그친다.
- **상태 전이**: (계산값)
- **반례**: 지급액과 같지 않다 — 본인 부담액과의 상계가 남아 있다.

### 한도 초과 청구

- **정의**: 부득이한 사유로 한도를 넘은 부분의 **지급을 청구**하는 것(`TRV-12-1`). 이 청구가 있으면 한도 적용 금액 단계가 **한 단계 올라가고**(`APV-7-1`), 초과를 넣은 총액 단계와 더하지 않고 높은 쪽을 쓴다(`APV-7-4`) — 초과는 결재선을 **한 번만** 움직인다.
- **채택 출처**: #5 결정 5("한도 초과가 결재권자를 한 단계 올리는 연쇄")를 #16이 조항으로 저작한 것. **직급이 정한 한도가 직책이 정한 결재선에 닿는 유일한 통로**다.
- **상태 전이**: 사전승인에서 인정되면, 그 인정 금액 안에 있는 정산은 다시 청구로 보지 않는다(`TRV-12-3`).
- **반례**: 단순한 `한도 초과`와 다르다. 초과분을 본인이 부담하면 청구가 아니고 결재선도 움직이지 않는다.

### 본인 부담액

- **정의**: 여비로 지급하지 않아 출장자가 지는 금액. 회사가 먼저 결제했으면 **반환**한다(`TRV-11`).
- **채택 출처**: 저작(#16). 지급하지 않음으로써 기본통칙 19-19…22가 말하는 "초과하는 부분의 금액은 원칙적으로 … 급여로 한다"의 경로를 여는 일을 피한다 `[1차·기관]`.
- **상태 전이**: 같은 정산에서 지급액과 상계된다(`TRV-11-2`). 순액이 음수면 지급 처리 뒤 **`반환 대기`**에 머물고, 재무팀의 반환 확인으로 닫힌다([ADR-0028](docs/adr/0028-negative-net-goes-to-repayment-pending.md)). **반환의 방법과 시기는 아직 정해지지 않았다 — 규정의 몫이고 #20이 정한다.**
- **반례**: `급여 공제`와 같은 말이 아니다. 공제는 회수 방법의 하나일 뿐이고, 이 저장소는 회수 방법을 아직 고르지 않았다.

### 동행

- **정의**: 같은 출장 목적으로 같은 일정의 출장을 수행하는 둘 이상의 출장자 관계. **각자 기안하고 각자 정산한다**(`TRV-3-6`).
- **채택 출처**: 저작(#16). KHIDI 위임전결기준 별표는 2인 이상 출장의 전결권자를 "출장자 중 최상급자를 기준으로" 정하지만 `[1차·기관규정]`, 이 회사는 동행자의 소속 본부가 갈릴 수 있어(하노버 전시회 = 해외영업팀 + 선행개발팀) 채택하지 않았다.
- **상태 전이**: (정적) 사전승인 문서에 적힌다.
- **반례**: 한 건의 공동 기안이 아니다. 동행이 결재선을 합치지 않으며, 여비에서는 숙박비 상한 하나에만 닿는다(`TRV-7-1`).

### 기한

- **정의**: 이 저장소에서 기한은 **저장된 날짜가 아니라 계산된 값**이다. 기준일·방향·영업일 수로 정의하며, 한 문서에 여러 기한이 걸리면 **가장 이른 것**이 그 문서의 기한이다(`PRC-3`). 모든 기한은 **기한일 18:00(대한민국 표준시)**에 끝나고, 출장지에서 한 제출도 한국 시간으로 판정한다(`PRC-3-6`). 사내 기한은 다섯이다 — 사전승인 기안·사전승인 완료·정산 기안·결재 처리·지급.
- **채택 출처**: #15 — 국세기본법 §5①의 휴일 보정 때문에 기한은 계산값이어야 한다 `[1차·법령]`. 종류와 값은 #16이 저작했다(`PRC-5`·`PRC-6`·`PRC-10`·`PRC-14`·`PRC-16`). 마감 시각과 판정 시각대는 2026-09-21 #16 교차 대조 결정(D-4)으로 Codex 레인의 기한 조항에서 가져왔다.
- **상태 전이**: (계산값) 기준일이 바뀌면 다시 계산된다.
- **반례**: **`법정 시계`(아래)와 같은 말이 아니다.** 법정 기한의 원문은 법령이고, 사내 규정은 그것을 다시 적지 않는다. 사내 기한을 법정 제재의 근거로 쓰지도 않는다([ADR-0004](docs/adr/0004-three-policy-documents.md), `PRC-15-2`).

### 영업일

- **정의**: 회사의 휴일이 아닌 날. 회사의 휴일은 토요일·일요일·「공휴일에 관한 법률」에 따른 공휴일 및 대체공휴일·「노동절 제정에 관한 법률」에 따른 노동절이다(`PRC-3-2`).
- **채택 출처**: 저작(#16). 국세기본법 §5①이 기한 특례에서 드는 세 가지와 **같은 집합**을 골랐다 `[1차·법령]` — 사내 기한과 세법 기한의 달력이 어긋나지 않게 하기 위해서이고, 근거로 삼았다는 뜻은 아니다.
- **상태 전이**: (정적) 연도별 공휴일 달력은 규정이 갖지 않고 밖에서 받는다.
- **반례**: `근무일`·`평일`과 섞지 않는다. 출장지의 휴일은 기한 계산에 쓰지 않는다(`PRC-3-4`).

### 법정 시계

- **정의**: 법령이 정한 신고·납부 기한. 원천징수(다음 달 10일)·부가가치세(25일)·법인세(3개월) 등이고, 한 출장 건이 여러 시계에 동시에 걸린다. **이 MVP는 법정 시계를 계산하지도 표시하지도 않는다.**
- **채택 출처**: 각 기한은 법령이 정한다 `[1차·법령]`. #15는 한 건이 여러 시계에 겹친다는 데서 "기한은 단수가 아니라 복수여야 한다"는 설계 함의를 끌어냈다. 계산·표시하지 않는 것은 #7의 범위 선택이고 #15의 설계 함의에서 **의도적으로 벗어난** 것이다 — 이 MVP는 신고·납부를 하지 않아 그 시계에 따라 움직일 행위자가 없다([PRD](docs/PRD.md) MVP 제외 사항 7).
- **상태 전이**: (이 MVP의 범위 밖)
- **반례**: **`기한`과 같은 말이 아니다** — `기한`은 사내 기한 다섯이고 원문은 출장관리규정이다. 법정 시계를 승인자 큐의 기한 축에 넣지 않고([PRD](docs/PRD.md) MVP 제외 사항 7), 사내 기한을 법정 제재의 근거로 쓰지도 않는다(`PRC-15-2`).

### 지출확인서

- **정의**: 증빙을 받을 수 없었던 지출에 대해 출장자가 작성하여 증빙을 갈음하는 문서. 갈음할 수 있는 금액에 출장 1건당 상한이 있다(`PRC-12-3`).
- **채택 출처**: 저작(#16).
- **상태 전이**: 정산 기안에 첨부된다. 상한을 넘는 부분은 지급하지 않는다(`PRC-12-4`).
- **반례**: **증빙이 아니다.** 법정 증빙 수취 의무를 대신하지도 않는다 — 사내 의무만 충족시킨다.

### 증빙

- **정의**: 지출이 실제로 있었음을 보이는 문서 — 영수증, 세금계산서, 카드 매출전표, 탑승권 등.
- **채택 출처**: 적격증빙 4종(신용카드 매출전표·현금영수증·세금계산서·계산서)은 **법인세법 §116② 1~4호** `[1차·법령]`. #15가 원문으로 확인했다(`docs/research/travel-evidence-legal-requirements.md` §1.1).
- **상태 전이**: `제출` → (전처리·추출) → `검산` → `대사` → [추출 불확실 건] `수기 확정` → `법정 판정` → `정산에 반영`
- **반례**: `영수증`과 동의어가 아니다. 영수증은 증빙의 한 종류다.

### 증빙 유형

- **정의**: 증빙을 지면에서 가를 때 쓰는 문서의 종류. 여섯으로 나눈다 — `세금계산서` / `계산서`(면세) / `신용카드매출전표` / `현금영수증` / `간이영수증` / `해외 증빙`. 어디에도 들지 않으면 `기타`다.
- **채택 출처**: 적격증빙 4종의 목록은 법인세법 §116② 1~4호 `[1차·법령]`, 지면에서 넷을 가르는 단서(세액란의 유무, 거래구분 문자열, 세액 구분 인쇄)는 #19 `[1차]`. 판별 규칙은 #18(`docs/design/receipt-pipeline.md` §2.2).
- **상태 전이**: 지면 단서에서 **코드가 유도**하고, 단서가 서로 어긋나면 `추출 불확실`이 되어 사람이 확정한다.
- **반례**: **`영수증 없음`을 증빙 유형으로 세지 않는다** — 그것은 카드 명세에 짝이 없는 `대사`의 결과다. 유형이 곧 `적격/비적격`을 정하지도 않는다. 판정은 `수취 의무 판정`이 따로 한다.

### 필요적 기재사항

- **정의**: 증빙이 법적으로 갖춰야 하는 항목. 이것이 빠진 증빙은 **읽기 실패가 아니라 문서 자체의 하자**다.
- **채택 출처**: 세금계산서는 **부가가치세법 §32① 1~4호** — ① 공급자의 등록번호와 성명·명칭 ② 공급받는 자의 등록번호 ③ 공급가액과 부가가치세액 ④ 작성 연월일 `[1차·법령]`. **계산서에는 세액란이 없고**(소득세법 시행령 §211①), 신용카드매출전표는 **공급가액과 세액이 별도로 구분**돼 있어야 매입세액 공제 요건을 충족한다(부가가치세법 §46③ · 시행령 §73⑧) `[1차·법령]`. 로캘마다 다르다는 것은 #19가 확인했다. 판정 규칙은 #18(`docs/design/receipt-pipeline.md` §6).
- **상태 전이**: (판정 입력)
- **반례**: **추출 실패와 구별한다.** #18의 구별을 그대로 쓴다 — 못 읽은 것(`추출 불확실`)은 사람이 수기 확정하지만, **요건 결손은 사람이 고칠 수 있는 것이 아니라 에이전트가 판정해 승인 카드에 올린다.** 그리고 **세액란이 비어 있는 것과 서식에 없는 것을 같이 읽지 않는다** — 계산서에 세액이 없는 것은 정상이다.

### 공급 장소

- **정의**: 재화·용역을 **공급받은 장소**. 국내이거나 국외이고, 지면으로 가릴 수 없으면 가릴 수 없다는 사실을 그대로 남긴다.
- **채택 출처**: #15 `[1차·법령]` — 법인세법 시행규칙 §79 4호가 "**국외에서** 재화 또는 용역을 공급받은 경우"를 수취 의무에서 빼고, 용역의 공급장소는 역무가 제공되는 장소다(부가가치세법 §20①1호).
- **상태 전이**: (판정 입력) — 공급자의 주소·등록번호 체계, 또는 카드 명세의 가맹점 소재국에서 정한다.
- **반례**: **결제 통화로 정하지 않는다.** 요건은 공급 장소이지 결제 통화가 아니며, 국내 여행사에서 산 항공권처럼 "해외 출장인데 국내에서 공급받은" 거래가 실재한다(#15 §2).

### 수취 의무 판정

- **정의**: 한 지출이 적격증빙을 받아 두어야 하는 대상인지에 대한 판정. 값은 셋이다 — **`의무 있음` / `의무 면제` / `판정 불가`**. `의무 있음`은 다시 충족·미충족으로 갈린다.
- **채택 출처**: #15 `[1차·법령]` — 법인세법 §116②(수취 의무), §75의5①(미수취 가산세 2%), 시행령 §158②1호(건당 3만원 이하 면제)·§158④(법인카드 명세가 전표를 갈음)·§158①2호 단서(읍·면지역 간이과세자로서 가맹점이 아닌 사업자 제외), 시행규칙 §79(국외 공급·택시·항공 항행용역·철도 여객·통행료 면제).
- **상태 전이**: `확정 증빙` → 세 값 중 하나. 치유(법인세법 §116③ 매입자발행)가 들어오면 다시 계산된다.
- **반례**: **`적격/비적격` 두 값으로 접지 않는다** — 접으면 출장 경로의 대부분이 하자로 찍힌다. 그리고 **사내 제출 의무와 같은 축에 두지 않는다.** 사내 층은 #16의 출장관리규정이 정하고, 이 판정은 법정 층만 본다.

### 법적 하자

- **정의**: 읽기에는 성공했는데 **문서 자체가 법정 요건에 미달**한 상태. 둘로 갈린다 — `수취 의무 미충족`과 `기재사항 결손`.
- **채택 출처**: #18 — #19가 갈라 둔 불확실성 2종 중 (ii). 조문은 위 `필요적 기재사항`과 `수취 의무 판정` 항목에 있다.
- **상태 전이**: `법정 판정` → **승인 카드의 주의 신호**. `치유 경로`(지출증빙 전환, 매입자발행세금계산서)가 붙는 경우가 있고, 치유되면 판정이 다시 돈다.
- **반례**: `추출 불확실`과 **같은 플래그로 묶지 않는다.** 다시 찍어 와서 해결되는 것이 아니다. 실패가 아니라 **판정 결과**이며, 증거 지표가 실제로 측정되는 자리다.

### 검산 / 검산 실패 / 검산 불가

- **정의**: 증빙에 인쇄된 숫자들이 서로 맞는지 **구조적으로** 확인하는 **행위**. 결과는 세 갈래다 — **검산 성공**(맞음) / **검산 실패**(적용할 공식이 있는데 안 맞음) / **검산 불가**(그 증빙에 적용할 공식을 우리가 갖고 있지 않음).
- **채택 출처**: #19 — 불확실성을 표현하는 세 경로 중 둘이 닫혔다. Claude API는 logprobs를 제공하지 않고, 자기보고 신뢰도는 단독 게이트로 쓸 수 없다. **남는 것은 구조적 검산이고, 그 공식은 로캘 의존이다.** #19가 확보한 것은 프랑스 `service compris`(서비스료가 subtotal에 포함), EU 소액 간이 세금계산서의 세액 분리 기재 면제 `[2차]`, 일본 적격청구서의 세율별 8%/10% 구분 기재다.
- **상태 전이**: `추출 완료` → `검산 성공` / `검산 실패` / `검산 불가`
- **반례**: **`검산 불가`를 `검산 실패`로 뭉개지 않는다.** 규칙이 없는 것과 규칙을 어긴 것은 후속 처리가 다르다. 그리고 **국가 단위로 "규칙 확보/미확보"를 가르지 않는다** — 확보 여부는 `로캘 × 증빙 유형` 단위다. 일본은 적격청구서에 한해 근거가 있을 뿐 모든 일본 영수증이 검산 가능한 것이 아니고, 독일에 대해 #19가 확보한 것은 **숫자 표기 로캘**(`1.234,56`)과 EU 소액 문서의 일반적 주의사항뿐이며 독일 고유의 검산 공식은 없다. 적용 가능한 규칙 집합은 #18이 확정했다(`docs/design/receipt-pipeline.md` §4.2), 그리고 거기서 규율이 하나 더해졌다 — **`검산 실패`를 선고할 수 있는 규칙은 1차 근거를 가진 것뿐이고**, `[2차]` 근거의 규칙은 확인만 하거나 `검산 불가`로 남는다.

### 추출 불확실

- **정의**: 지면의 값을 **확정하지 못한 상태**. 모델이 발화한 상태값(`읽음`·`구분자 모호`·`날짜 로캘 모호`·`판독 불가`·`지면에 없음`)과 코드의 규칙·검산·대사 신호로 만들어진다.
- **채택 출처**: #19 — 불확실성을 표현하는 세 경로 중 둘(logprobs·자기보고 신뢰도)이 닫혔고 남은 것이 구조적 검산이다. 결정은 [ADR-0008](docs/adr/0008-extraction-uncertainty-is-first-class.md).
- **상태 전이**: `추출` → (규칙이나 대사로 해소) **닫힘** / (판정 입력 필드면) `수기 확정` → **닫힘**. **정책 대조 이전에 반드시 닫힌다.**
- **반례**: `검산 불가`와 같지 않다 — 규칙이 없는 것은 값을 못 읽은 것이 아니다. `법적 하자`와도 같지 않다. 그리고 **신뢰도 점수가 아니다** — 값이 아니라 모름의 종류를 이름으로 구분한다.

### 대사

- **정의**: 법인카드 명세의 한 줄과 제출된 증빙 한 장을 짝지어 같은 지출임을 확인하는 것.
- **채택 출처**: #18 — 불확실한 건을 **사람이 직접 확정해 입력하는 시점**이 바로 여기다. 판정 키는 #18이 다시 설계했다(`docs/design/receipt-pipeline.md` §7): 승인번호 → 카드 끝 4자리 + **이용금액(원거래 통화)** + 날짜 창 → 가맹점 사업자등록번호 → 가맹점명. 청구액이 아니라 이용금액을 쓰는 것이 할부를, 날짜 창이 시차를 넘게 한다.
- **상태 전이**: `카드 명세 + 증빙` → `대사 완료` / `대사 불일치` / `짝 없음`. **자동으로 `대사 완료`가 되는 것은 경쟁 후보 0(양방향) · 해결되지 않은 추출 문제 0 · 값끼리의 모순 0을 모두 만족할 때뿐이다** — 대사가 카드 줄 값으로 후보 집합을 정확히 하나로 좁힌 값은 해결된 것으로 친다. 그리고 카드 끝4·금액·날짜를 **셋 다** 실제로 대조해야 한다 — 하나라도 지면에 없으면 자동 확정하지 않는다(승인번호만으로 확정하는 길은 없다). **짝이 먼저, 차액은 그다음이다** — 같은 통화에서 금액만 어긋나면 `대사 불일치`이고, 금액이 어긋나면서 다른 막힘도 있거나 통화가 다르면 같은 거래인지부터 확인한다(`짝 보류`). 자동으로 묶이지 않는 중간 상태가 둘 있다 — 후보가 둘 이상 남으면 `후보 복수`, 후보가 하나지만 자동 확정 조건에 막히면 `짝 보류`(막힌 조건을 함께 남긴다). 확인하는 사람은 막힌 이유가 정한다 — 값 모순이면 재무합의자, 그 밖에는 기안자다. 둘 다 사람이 고르면 `대사 완료` · `대사 불일치` · `짝 없음` 중 하나로 닫힌다. 통화가 다르면 차액 대신 두 금액(증빙 금액·카드 줄 원거래 금액)을 남긴다 — 사람이 같은 거래로 확인하면 `대사 불일치`로 닫히고, 원화 환산은 #16이 한다.
- **반례**: `매칭`이라 쓰지 않는다. 그리고 **`영수증 없음`은 증빙 유형이 아니라 카드 쪽 `짝 없음`이다** — 법인카드 결제분은 명세서가 전표를 갈음하므로(법인세법 시행령 §158④) 그 자체로 법적 하자가 아니다.

### 수기 확정

- **정의**: 사람이 원본을 보고 값을 직접 입력해 `추출 불확실`을 닫는 행위. **에이전트가 읽은 값을 화면에 미리 채우지 않고**, 확정값으로 검산·대사를 다시 돌린다.
- **채택 출처**: #18 — 불확실한 건은 자동 추론으로 메우지 않고 대사 시점에 사람이 확정하며 그 개입이 감사 로그에 남는다. 미리 채우지 않는 이유는 #19가 인용한 측정(승인 프롬프트의 약 93%를 사람이 승인한다) `[1차]`.
- **상태 전이**: `추출 불확실` → (입력) `확정` / (사람도 지면에서 읽지 못함) `재촬영 요청` 또는 `확정 불가`. `확정 불가`는 열린 상태가 아니라 닫힌 상태이고, `수취 의무 판정`에서 `판정 불가`의 입력이 된다.
- **반례**: `승인`이 아니다 — 결재선의 행위가 아니라 값의 출처를 바꾸는 입력이다. **에이전트 값에 대한 동의도 아니다**: 사람의 입력과 에이전트의 판독값은 따로 기록되어 지표가 된다.

---

# §3 시스템 개념

업무 용어는 아니지만 이슈 제목·테스트 이름·화면 문구에 계속 나올 말이다. 고정하지 않으면 표현이 흔들린다.

### 행위자 / 책임자

- **정의**: **행위자**는 그 조작을 실제로 수행한 주체(사람 또는 에이전트), **책임자**는 그 결과에 책임을 지는 사람. 둘은 다를 수 있고, 감사 기록은 **둘을 따로** 남긴다.
- **채택 출처**: #5 결정 7 — "에이전트가 기안·결재를 대신 올리는 순간 '에이전트=행위자, 전결권자=책임자'를 남기지 않으면 감사에 답할 수 없다". 벤더 근거는 하이웍스가 유일하다 `[1차·벤더]` — "전결로 결재 완료하면 **책임은 전결한 사람에게** 있습니다" / "대결로 최종 결재를 완료시키면 **책임은 처리한 사람과 대상 모두에게** 있습니다".
- **상태 전이**: (모든 기록 이벤트에 동반)
- **반례**: `approver` 하나로 합치지 않는다. 에이전트가 대신 입력한 값의 책임은 에이전트에게 있지 않다. **에이전트는 결재 행위(승인·동의·반려·보류)의 행위자가 되지 않는다** — 위 #5 인용은 에이전트가 결재를 대신하는 경우까지 상정했지만, 이 저장소는 그 경우를 두지 않는다(PRD 그릴링, 2026-09-21 결정). 결재선은 전결표가 정한 사람 — 전결 단계의 직책자(`APV-3`)와 재무합의자(`APV-9-2`) — 으로만 이뤄지고 에이전트에게 다시 위임하는 조항이 없다. 에이전트가 행위자가 되는 것은 증빙 분류·추출, 여행사 메일 판독, 기안 초안, 보류 답변처럼 결재 밖의 읽기와 서술이고(대사·계산은 코드다), 그 경계는 [ADR-0018](docs/adr/0018-two-model-contracts-no-side-effect-tools.md)이 정했다. 비결재 조작의 책임자는 [ADR-0019](docs/adr/0019-non-approval-owner-is-assigned-operations-person.md)가 정했다.

### 값의 출처

- **정의**: 정산 한 건의 **필드마다** 그 값이 어디서 왔는지. 세 가지뿐이다 — **에이전트 추출** / **사람 수기 확정** / **시스템 연동**.
- **채택 출처**: #18 — #5 결정 7의 행위자/책임자 축에 "사람이 손으로 고친 값"이 세 번째 출처로 들어간다.
- **상태 전이**: 필드에 값이 쓰이는 순간 함께 기록되며, 덮어쓰이면 출처도 함께 바뀐다(이전 값은 감사 로그에 남는다). **같은 사실을 시스템 연동이 갖고 있으면 그 값이 판정의 입력이 되고**, 에이전트 추출값은 대사의 비교 대상으로만 쓴다(#18).
- **반례**: 출처를 건 단위로 기록하지 않는다. **필드 단위**다 — 한 영수증에서 금액은 에이전트가 읽고 날짜는 사람이 확정하는 일이 실제로 생긴다.

### 모의 커넥터

- **정의**: 한성워크·ERP·여행사·법인카드사 같은 실 시스템을 대신해 **합성 데이터를 내놓는 연동**. 이 MVP는 실 시스템 대신 이것을 부른다.
- **채택 출처**: #1 Out of scope — 실연동은 도입 단계의 문제이지 청사진의 문제가 아니고, 가상 회사·합성 데이터라는 무대와도 맞지 않는다. 흉내 낼 후보는 as-is 4계통이다([`docs/company/tools.md`](docs/company/tools.md)). **이 MVP의 모의 커넥터는 셋이고 모두 들어오기만 한다** — 법인카드사 웹·여행사 메일함·한성워크 조직 정보 스냅샷. ERP·회계는 흉내 내지 않는다([ADR-0021](docs/adr/0021-three-read-only-mock-connectors-no-erp.md)).
- **상태 전이**: (무대)
- **반례**: **값의 출처에 새 칸을 만들지 않는다** — 모의 커넥터가 넘긴 구조화된 값(법인카드 명세의 한 줄 등)은 `시스템 연동`이다. 모의라는 것은 무대의 성질이지 값의 성질이 아니고, 실연동으로 바꿔도 기록이 그대로여야 한다(#7 후속, 2026-09-21 결정). 같은 이유로 모의 커넥터가 넘긴 **문서**(여행사 이메일)에서 에이전트가 읽은 값은 `에이전트 추출`이다. 실연동이라 부르지 않는다 — 성공 기준은 모의 커넥터의 결과 위에서만 관측한다([PRD](docs/PRD.md) 성공 기준).

### 검증 수준

- **정의**: 한 필드의 값이 **무엇으로 확인됐는지**. 셋이다 — `교차 확인`(대사로 시스템 연동 값과 맞음) / `검산 확인`(구조적 검산 규칙을 통과) / `단독 판독`(둘 다 없음).
- **채택 출처**: #18 — #19가 닫은 두 경로(logprobs·자기보고 신뢰도) 대신 남은 수단이 구조적 검산과 교차 확인이므로, **무엇으로 확인했는지가 곧 값의 근거**다.
- **상태 전이**: `검산`·`대사`가 끝나면 정해지고, `수기 확정`이 값을 바꾸면 다시 계산된다. 합계처럼 여러 값을 더한 자리는 구성 요소의 **최저 수준**을 따른다.
- **반례**: 신뢰도 점수가 아니다 — 숫자가 아니라 확인 수단의 이름이다. 행동을 요구하는 플래그도 아니다: 증빙 파이프라인이 승인 카드에 올리는 주의 신호는 `법적 하자`와 `수취 의무 판정`의 `판정 불가` 둘뿐이고, `단독 판독`은 필드 표에 적힐 뿐이다.

### 감사 로그

- **정의**: 누가·무엇을·언제 했는지와, **원래 에이전트가 읽은 값은 무엇이었는지**를 남기는 기록. 사람이 수기 확정한 지점이 특히 여기 남는다.
- **채택 출처**: #18.
- **상태 전이**: 추가만 되고 지워지지 않는다.
- **반례**: 일반 애플리케이션 로그와 구별한다. 감사 로그는 **증거**이고, #19가 경고한 대로 `사람이 확인함`이라는 기록은 **건수가 적을 때만** 증거로서 값을 갖는다.

### 이의 플래그

- **정의**: 내부감사(admin)가 어떤 건에 대해 "다시 보라"고 표시하는 것. admin의 권한은 **둘로 쪼개져 있다** — **값을 수정하거나 원복할 수는 없고**, **재심 절차를 개시할 수는 있다**. 감사자는 판정하지 않고 되물을 뿐이지만, 정산에서는 되묻는 행위 자체가 상태를 움직인다. 사전승인은 상태를 바꾸지 않는다([ADR-0027](docs/adr/0027-reopen-returns-to-first-finance-consent.md)).
- **채택 출처**: 이 저장소의 결정([ADR-0002](docs/adr/0002-admin-read-only-audit-axis.md)). #18은 감사자 축이 생긴다는 것만 확정했고 권한 범위는 #6으로 넘겼다.
- **상태 전이**: 정산은 `완료` 또는 `진행중` → (플래그) → **`재무합의대기`**의 첫 단계. 결재권자 범위의 쟁점이면 원 결재선까지 다시 본다. 바뀐 것 없이 닫히면 플래그 직전의 대기 또는 원 `완료`와 원 완료 시각으로 돌아가고, 재심 중에는 지급 처리를 막는다. 지급 처리 뒤에 건 플래그와 그 복귀는 ADR-0027의 설계 가정을 따른다. 수정값은 새 필드 revision으로 `대사`를 다시 거친다. 사전승인은 상태를 바꾸지 않는다 — 진행 중이면 현재 결재권자의 대기에 재심 표지를 붙이고, 완료 건은 플래그를 그 출장의 정산 카드로 넘긴다([ADR-0027](docs/adr/0027-reopen-returns-to-first-finance-consent.md)).
- **반례**: `반려`가 아니다. 반려는 승인자의 행위이고 기안자에게 돌아가지만, 이의 플래그는 **감사자의 행위**이고, 정산은 재무합의로 돌아가며 사전승인은 상태를 바꾸지 않는다. admin은 결재선에 서지 않으며, 승인자의 액션 3종([ADR-0003](docs/adr/0003-approver-actions-three.md))에 이것을 네 번째로 더해 세지 않는다.

### 승인 카드

- **정의**: 승인자 큐에서 한 건을 펼쳤을 때 보이는 판정 단위. 본문은 자연어 요약이 아니라 **구조화 필드 + 정책 원문 인용**이다.
- **채택 출처**: #13 — 실물 화면 조사에서 승인 카드 본문은 어디서도 자연어 요약이 아니라 구조화 필드 또는 툴 호출 인자 원본이었다. 빈칸은 그 칸을 채울 **기계 대조 가능한 정책 원문**이다.
- **상태 전이**: (표시 단위)
- **반례**: "요약 카드"가 아니다. **판정 권고(승인 권고·반려 권고)를 싣지 않는다** — 권고는 승인자의 결정을 미리 채우는 것이고, 수기 확정이 에이전트 값을 미리 채우지 않는 것과 같은 이유다(PRD 그릴링, 2026-09-21 결정).

### 주의 신호

- **정의**: 승인 카드에서 승인자에게 **따로 판단을 요구하는** 표시. 증빙 파이프라인이 올리는 주의 신호는 **둘뿐이다** — `법적 하자`와 `수취 의무 판정`의 `판정 불가`. 두 칸을 합치지 않는다.
- **채택 출처**: #18 — 사람에게 보내는 건수 자체가 보장의 분모이므로(#19) 신호의 종류를 구조로 묶었다([ADR-0010](docs/adr/0010-human-routing-bounded-by-structure-measured-by-ratio.md), 설계 문서 §10의 H11). 정책 대조 결과는 카드의 별도 구획이고(#7 결정 D-16), **주의 신호가 아니다** — [#8 결과 4](https://github.com/DHChe/E2EAI_business_proj/issues/8#issuecomment-5769586450).
- **상태 전이**: `법정 판정` → 주의 신호. 치유로 판정이 다시 돌면 다시 계산된다.
- **반례**: **`검증 수준`이 아니다** — `단독 판독`은 필드 표에 적힐 뿐 행동을 요구하지 않는다. **`추출 불확실`도 아니다** — 추출 불확실은 정책 대조 이전에 닫혀 승인 카드에 닿지 않고, 기안자가 수기 확정 화면에서 본다. 신뢰도 점수도 아니다.

### 승인자

- **정의**: 결재선에 서서 기안 문서를 판정하는 사람 — **결재권자와 재무합의자를 함께** 가리킨다. 승인자 큐의 주인이고, 판정 액션은 세 가지다 — 결재권자는 `승인`·`반려`·`보류`, 재무합의자는 `동의`·`반려`·`보류`.
- **채택 출처**: #7 결정 D-3 — PRD의 1차 사용자를 정하며 두 역할을 한 말로 묶었다. 묶을 수 있는 근거는 [ADR-0003](docs/adr/0003-approver-actions-three.md)의 "역할과 액션은 다른 축"이다 — 두 역할이 같은 세 자리의 액션을 수행한다. **ADR-0003의 결정 문장과는 이름 하나가 다르다** — ADR은 세 액션을 `승인`·`반려`·`보류`로 적었지만, 재무합의자의 첫째 자리는 #16이 `동의`로 이름 붙였다(`APV-9-5`). 이 용어집은 조항의 이름을 따른다.
- **상태 전이**: (역할의 묶음)
- **반례**: **결재선에 적히는 역할 이름이 아니다** — 결재선에는 `결재권자`·`재무합의자`가 적히고, 이 말은 둘을 함께 부를 때만 쓴다. **기안자와 내부감사(admin)는 승인자가 아니다** — admin은 결재선에 서지 않는다(`APV-3-3`). 재무합의자의 첫째 액션을 `승인`이라 부르지 않는다 — `동의`다.

### 승인자 큐

- **정의**: 한 승인자가 처리해야 할 승인 카드의 목록. 이 MVP의 **척추**다.
- **채택 출처**: #5 결정 9 — "여러 건이 기한 순으로 쌓인 인박스가 먼저이고, 한 건을 열면 그 상세가 펼쳐진다".
- **상태 전이**: (뷰)
- **반례**: `인박스`라 부르지 않는다. #13이 확인한 세 제품 어디에도 **기한으로 정렬하거나 묶는 화면이 없었고**, #5 결정 6이 기한을 1급 속성으로 정했으므로 이 큐는 기한 축을 갖는다.

---

## 아직 이 용어집에 없는 것

말이 아직 고정되지 않았다는 뜻이고, 그 자체가 신호다(`docs/agents/domain.md`).

- 규정의 **조항 식별자 · 재량 조항 · 판(版)**을 §3 시스템 개념에 올릴지 — #16이 [ADR-0005](docs/adr/0005-policy-source-of-truth.md)로 규칙을 정했으나, 이 티켓의 편집 범위가 §1·§2였다
