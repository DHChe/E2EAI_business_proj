# #10 검수 수정 재확인 — codex

- 기준 HEAD: `ed1ac443677412a57a04b665aec2d0ec6cd0d38f`; 작성자 수정 `f3b4b482f90db03d9050c696183057a2194be45d`와 그 부모 `f43d3c8`을 대조했다.
- 기준 목록: 지정된 `scratchpad/issue10/fix-list-1.md`의 1–21. 두 독립 검수 보고서는 지정된 `DHChe/frontier-10-review-{codex,grok}` 브랜치에서 읽었다.
- 아래 `비교 문서`는 `docs/research/agent-architecture-comparison.md`, `초안`은 `/private/tmp/claude-501/-Users-astralpig-DEV-portfolio-E2EAI-business-proj/71856148-47a6-4f96-95fc-5427a6865b83/scratchpad/issue10/gh-drafts/`를 뜻한다. 줄 번호는 현재 HEAD 및 읽은 초안 기준이다.

## 판정

추가 수정 필요 — 저장소 수정은 병합 가능하지만, 함께 게시할 초안의 한정어·발견자 귀속 2곳을 먼저 고쳐야 한다.

## 항목별

| # | 판정 | 근거(파일:줄) |
| --- | --- | --- |
| 1 | 다르게 적용됨(타당) | `docs/ARCHITECTURE.md:253`, `:284-291`, `docs/adr/0019-non-approval-owner-is-assigned-operations-person.md:82-84`: 표를 결정으로 복원하고 예시·구현 형식만 가정으로 남겼다. 별도 미확정 행은 확인되지 않았다. |
| 2 | 적용됨 | `docs/ARCHITECTURE.md:261`: 출장 일정 반영과 인사 담당을 복원했다. `:265`는 한성워크 조직 동기화만 유지하고, 기한은 `:294` 이하에서 별도로 다룬다. |
| 3 | 적용됨 | `docs/ARCHITECTURE.md:371`: 현재 단계의 승인·동의를 막고 재무합의자가 확인하며 반려·보류는 받는다. `:315` 및 비교 문서 `:193`과 일치한다. |
| 4 | 적용됨 | `docs/adr/0012-transition-audit-provenance-in-one-transaction.md:54`, `:56`: 과거 문구를 취소선으로 남기고 현재 배정·도구 결정을 ADR-0019·0018로 연결했다. |
| 5 | 적용됨 | `docs/adr/0014-apv-9-3-exposed-declaratively.md:79`: 옛 인용을 취소하고 삭제된 항목과 현재 `CONTEXT.md:402`(#17)·`:403`(#16)을 구별한다. 실제 두 줄과 맞는다. |
| 6 | 적용됨 | `docs/ARCHITECTURE.md:126`: 주입 실패 근거는 `docs/design/receipt-pipeline.md:261`·비교 문서 `:80`, 메일이라는 사실은 `docs/company/tools.md:15`로 나눴다. |
| 7 | 적용됨 | `docs/ARCHITECTURE.md:443-444`, `:481`, 초안 `comment-18.md:9-10`: `docs/design/receipt-pipeline.md:1806`의 필수 `billedKrw` 공백을 넘기며 타입을 정하지 않는다. 초안은 13줄이다. |
| 8 | 적용됨 | `docs/ARCHITECTURE.md:346`: 재무합의자가 한 명 이상인 정산 그림으로 한정하고 `APV-9-4` 귀결은 #20으로 넘긴다. |
| 9 | 다르게 적용됨(타당) | `docs/ARCHITECTURE.md:127`: A1 위반과 사람 확인·반복 확인·보상의 복합 반증을 구별한다. 남긴 결재 이벤트 구절은 경계가 없을 때의 실패 설명이다. |
| 10 | 적용됨 | `docs/ARCHITECTURE.md:476`: `docs/company/approval-matrix.md:233`을 추가했고 `:216-217`과 함께 500만 원 이하 조건을 받친다. |
| 11 | 적용됨 | `docs/ARCHITECTURE.md:215`, `docs/adr/0020-hold-answer-slots-and-server-templates.md:68`, 초안 `issue10-update2.md:17`: `stopReason`과 API 필드 `stop_reason`을 구별했다. SlipScan `lib/claude/report.ts:144-148`, `:201`과 일치한다. |
| 12 | 적용됨 | `docs/design/receipt-pipeline.md:2348`: ADR-0021 결정 1의 한성워크 조직 스냅샷으로 근거를 좁혔다. 실제 `docs/adr/0021-three-read-only-mock-connectors-no-erp.md:36`과 맞는다. |
| 13 | 적용됨 | `docs/ARCHITECTURE.md:137`, `docs/adr/0021-three-read-only-mock-connectors-no-erp.md:66`: A8·`docs/PRD.md:24`를 직접 근거로 두고 ADR-0011의 SQL/Temporal 이중 상태는 유추라고 명시한다. |
| 14 | 적용됨 | `docs/adr/0019-non-approval-owner-is-assigned-operations-person.md:53`: 중복 조사 제거. |
| 15 | 적용됨 | `docs/PRD.md:173`: #9의 ADR 0011–0017·0014와 #10의 개정권자 ADR-0022로 구별했다. |
| 16 | 다르게 적용됨(타당) | `docs/company.md:97`: “#10이 정했다 → ADR-0021”로 닫힌 상태를 명시한다. |
| 17 | 적용됨 | `docs/company/tools.md:7`: 모의 범위 ADR 포인터를 붙였으며 월 단위 수동 다운로드인 `:16`은 그대로다. |
| 18 | 적용됨 | `docs/adr/0021-three-read-only-mock-connectors-no-erp.md:94`: 두 완료 포인터 수정과 as-is `tools.md:16` 보존을 사실대로 설명한다. |
| 19 | 적용됨 | `docs/ARCHITECTURE.md:475`, 초안 `comment-20.md:4`: 선행 재량 기록 가능 여부가 미정인 조건부 교착 위험으로 고쳤다. |
| 20 | 적용됨(초안 잔여 문제 있음) | 초안 `comment-18.md:7`에 지정 문장은 들어갔다. 다만 같은 줄 앞의 담당 공백 단정은 그대로여서 아래 게시 초안 P1의 보완이 필요하다. |
| 21 | 적용됨 | `docs/ARCHITECTURE.md:476`, `docs/adr/0019-non-approval-owner-is-assigned-operations-person.md:85-86`, 초안 `comment-20.md:6-7`: 재무합의 부재만 넘기고 대사 책임자가 본인이라고 하지 않는다. 비교 문서 `:238`의 해당 표현도 부정하는 문맥이다. |

## 다르게 적용한 것 (a)–(d)

- **(a) 타당.** `docs/ARCHITECTURE.md:127`은 표의 “이 경계가 없으면 생기는 구체적 실패” 칸이다. 에이전트 결재 이벤트가 허용된다는 현재 계약이 아니라, 경계 상실 시 금지된 사건이 생기는 실패를 말한다. `CONTEXT.md:331`·ADR-0012 `:35-36`과 모순되지 않는다. 이어진 문장은 업무 쓰기 위반만으로 ADR-0011 반증 1 전체가 성립하지 않음을 보존한다.
- **(b) 타당.** U1이 받은 codex §6.2 표와 현재 `docs/ARCHITECTURE.md:257-282`를 대조했다. U3·U4·U5·U6 및 작성자에게 맡긴 기한 처리 외에 새로 미확정 배정을 확정한 행은 없다. 복수 결재권자 `decided_by`는 `:278`에서 여전히 #20으로 남고, 실명 예시와 레코드 형식은 `:284-291`에 가정으로 표시돼 있다. 미확정 행이 없는데 표시할 필요는 없다.
- **(c) 타당.** 목록 자체가 허용한 “→ ADR-0021” 방식이며 `docs/company.md:97`은 완료 동사를 함께 써서 열린 항목으로 오인할 여지를 줄인다. 취소선 유무는 결정의 내용을 바꾸지 않는다.
- **(d) 타당.** ADR-0020 `:15`는 비교 문서 `:69`의 조사 기록을 가리키며, 실제로 그 줄에 같은 API 이름 표현이 있다. 코드의 저장 함수 식별자를 직접 인용하는 ADR-0020 `:68`과 ARCHITECTURE `:215`는 `stopReason`으로 고쳤다. 비교 문서 본문 보존이라는 범위 안에서 직접 코드 인용과 과거 조사 기록을 구별한 처리다.

## 새 결함

없음.

저장소의 두 수정 커밋에서 새로 넣거나 바꾼 `파일:줄` 인용과 축약 인용을 원문과 대조했다. 줄 어긋남·인용 합성·새 한정어 누락이나 주변 ADR과의 새 모순은 발견하지 못했다. 게시 초안에 남은 2건은 아래에 따로 적는다.

`ed1ac44` 부록(`docs/research/agent-architecture-comparison.md:232-242`)의 11행은 두 검수 보고서 §4의 판정 8건씩과 일치한다. codex의 부분 판정 4건(교착·시드 책임자·재무합의 소멸·여행사 카드)을 확인으로 격상하지 않았고, grok의 확인 8건과도 맞는다. 기존 사실과 레인 가정을 구별한 귀결 역시 일치한다.

`f3b4b48`의 비교 문서는 222줄이며, `ed1ac44`는 기존 줄 번호 **13·20·151·204·216만 제자리 수정**하고 223–242행을 덧붙였다. 1–222행의 위치 이동은 없다. 의뢰에서 말한 상태 줄 넷은 `:13`, `:20`, `:204`, `:216`이고, 추가로 `:151`의 §4 제목에 부록 안내를 붙인 것이다. 교차 검증 완료는 두 보고서로 확인했고, 원문 세 브랜치의 게시 완료는 `git ls-remote origin`으로 직접 확인했다: codex `97d1dd9`, grok `46e5540`, claude `a30c5f7`이 각각 원격 브랜치 끝이다.

## 게시 초안

초안 다섯 개의 `파일:줄`과 `:N` 축약 인용을 모두 읽어 대조했다. 저장소 인용은 `ed1ac44`, `slipscan:` 인용은 초안이 지정한 `/Users/astralpig/DEV/fc-Agentic-Workflow-prac`의 원문이다. 행 번호 자체의 어긋남은 없다. 문제는 2개 파일의 의미 한정·발견자 귀속 각 1곳이다.

| 초안 | 판정 | 확인 결과 또는 고칠 곳 |
| --- | --- | --- |
| `comment-17.md` | 맞음 | `CONTEXT.md:365`, `:88`, 전결표 `:205`, ADR-0003 `:25-26`, ADR-0011 `:35`, 비교 문서 `:162`, `:158` 모두 맞는다. 정의된 보류 복귀와 #17의 미정 전이를 구별하며 후속 결정을 대신하지 않는다. |
| `comment-18.md` | 고칠 곳 P1 | `:7`의 “그래서 … 규칙에 딱 맞지 않는 빈틈이 생긴다”는 카드 명의 부재만으로 담당 공백을 단정한다. 뒤에 추가한 “공용 카드 여부는 정해지지 않았다”가 이 앞 문장의 무조건적 인과를 해소하지 못한다. `docs/company/tools.md:36-38`은 명의를 정하지 않고 `CONTEXT.md:310`은 소유자가 아니라 막힌 이유로 담당을 정한다. codex 검수 §4도 모든 여행사 카드에서 담당이 없다고 단정하지 말라고 한 부분 판정이다. **“카드 명의와 문서 귀속에 따라 기존 짝 확인 담당 규칙을 적용할 수 있는지 #18 후속에서 확인한다”**로 바꾼다. 나머지 타입·업종·방향 자산 인용은 맞고 타입 해법을 미리 정하지 않는다. |
| `comment-20.md` | 맞음 | `:4`는 “교착할 수 있다”와 선행 판단 기록의 미정 조건을 모두 명시한다. 전결표 `:216-217`, `:233`은 500만 원 이하에서 다음 단계가 없는 조건과 맞는다. 비교 문서 `:143`, `:195`, `:161`, `:187`도 맞고 결재자 교체·재량 책임자를 새로 정하지 않는다. |
| `comment-22.md` | 맞음 | 비교 문서 `:193`, `:163`, `:199` 및 `CONTEXT.md:315`와 맞는다. 이미 정한 입력 출처와 아직 정하지 않은 용어 정의를 구별한다. |
| `issue10-update2.md` | 고칠 곳 P2 | `:29`의 “세 레인이 독립으로 찾았고”가 두 정정 모두를 묶는다. 근거로 든 비교 문서 `:66`은 조회가 촉발하는 쓰기를 세 레인 발견으로 기록하지만, `:69`는 계산 환각 주장을 반박한 레인을 **codex·claude 둘**로 한정하고 grok은 그대로 채택했다고 명시한다. **“②는 세 레인이, ①은 codex·claude 두 레인이 찾았고 코디네이터가 재확인했다”**로 나눈다. 이는 새로 만든 오류가 아니라 게시 전 전체 인용 대조에서 확인한 잔여 귀속 오류다. SlipScan의 함수·API 식별자, `:144-148`·`:201`·`:205-212`, 프롬프트 `:11`, UPDATE/DELETE `:167-204`, GET 라우트 줄, ADR-5 `:520`, 이 저장소 `travel-procedure.md:289`는 모두 원문과 맞는다. |

검수 대상 파일과 저장소 밖 초안은 수정하지 않았다. `graft map`이 0파일이고 `graft ask --source`에도 결과가 없어 지정된 diff와 필요한 원문 구간을 직접 읽었다. 구현·서비스 현황의 재검증이나 최초 설계의 재검수는 하지 않았다.
