# PRD

> **미작성.** 아래 항목이 채워져야 `/harness`로 phase를 설계할 수 있다.
> 이 문서가 채워진 뒤 `/domain-modeling`으로 `CONTEXT.md`(용어집)를 만든다.

## 목표

{이 프로젝트가 해결하려는 문제를 한 줄로}

## 사용자

{누가 쓰는지. 그 사람이 지금 이 문제를 어떻게 해결하고 있는지}

## 핵심 기능

1. {기능}
2. {기능}
3. {기능}

## MVP 제외 사항

{안 만들 것을 명시적으로 적는다. `/review`가 범위 초과를 여기 대조해 판정한다.}

- {제외 1}
- {제외 2}

## 성공 기준

{무엇이 관측되면 이 MVP가 성공인가. 검증 가능한 형태로.}
